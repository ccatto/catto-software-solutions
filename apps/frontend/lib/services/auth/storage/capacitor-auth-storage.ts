// Re-export from @ccatto/react-auth package
// Note: RLeaguez uses 'myapp' prefix — create instance with:
// new CapacitorAuthStorage({ keyPrefix: 'myapp' })
export { CapacitorAuthStorage } from '@ccatto/react-auth';
